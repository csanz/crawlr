This context file is a merged snapshot of these files (or folders):

- ./

Generated by AIContext cli utility for AI-based analysis and review.

File Guide:
-------------------
The content is organized as follows:
1. Objective, Usage and Notes
3. Directory and file paths structure
4. A file list where each file contains:
  a. A Header section containing:
      1. File Path (path/to/file)
      2. Number of lines
      3. Number of Tokens
      4. File Extension
      5. This will be optional, if blank then ignore.
  a. The header will be surrounded by (----------------------------------)
  d. A blank line and the full contents of the file
  e. A blank line and the next file

Objective
-------------------
To provide A read-only, combined view of source files for AI processing, debugging, or documentation.

Usage
-------------------
- File paths are included to separate and identify individual files.
- All content is read-only and provided for analysis only.
- Sensitive data may be included—handle with appropriate safeguards if stored or transmitted.

Notes
-------------------
- Binary files and ignored paths (e.g. .gitignore, config rules) are excluded.
- Referenced files not shown here may be intentionally omitted.

Directory Structure
-------------------

./
├── docs/
│   ├── Code Organization.md
│   ├── Tech Design Player Growth.md
│   └── Tech Design Slither Tail.md
├── libs/
│   ├── CameraSetup.js
│   ├── Coin.js
│   ├── CoinManager.js
│   ├── Collectible.js
│   ├── CollisionHandler.js
│   ├── GameLoop.js
│   ├── Ground.js
│   ├── InputHandler.js
│   ├── Lighting.js
│   ├── PhysicsConfig.js
│   ├── Player.js
│   ├── PushableBlock.js
│   ├── Radar.js
│   ├── RendererSetup.js
│   ├── SkySphere.js
│   ├── Sound.js
│   ├── Tail.js
│   ├── TextureLoader.js
│   └── three.min.js
├── public/
│   ├── assets/
│   │   ├── tail/
│   │   ├── block-texture.png
│   │   ├── ground-texture-2.png
│   │   ├── ground-texture.png
│   │   ├── player-texture-face.png
│   │   ├── player-texture.png
│   │   ├── sky-texture.png
│   │   ├── tail-texture.png
│   │   └── tail-texture.svg
│   ├── block-kill-2.mp3
│   ├── block-kill.mp3
│   ├── coin-collect-large.mp3
│   ├── coin-collect.mp3
│   ├── game-end.mp3
│   └── style.css
├── app.js
├── favicon.ico
├── index.html
├── package.json
├── README.md
└── vite.config.js


File(s)
-------------------

----------------------------------
File: README.md
Lines: 48
Tokens: 385
Extension: .md
Purpose: N/A
Last Modified: 4/21/2025, 2:37:00 AM
Size: 1.50 KB
----------------------------------

{{FileContent}}
```md
# Simple Slither Game

A simple 3D game built with Three.js and Rapier physics where you control a character that collects coins and grows a snake-like tail.

## Features

- 3D environment with physics-based interactions
- Player movement with keyboard controls
- Collectable coins that spawn randomly
- Dynamic tail that grows as you collect coins
- Physics-based interactions with environment

## Tail Implementation

The game features a dynamic tail system inspired by the classic Snake game:

- As you collect coins, your character grows a tail
- Each tail segment follows the path of the player with a delay
- Tail segments have a subtle wiggle effect for liveliness
- Custom shader and geometry for visually appealing segments
- Particle effects when new segments are added

## Technical Details

This game uses:
- Three.js for 3D rendering
- Rapier physics for realistic interactions
- Modular code architecture for maintainability
- Custom shader effects and procedurally generated textures

## Documentation

For more details on the implementation, see:
- [Tech Design: Slither Tail](docs/Tech%20Design%20Slither%20Tail.md) - Technical design of the tail system
- [Code Organization](docs/Code%20Organization.md) - Overview of the code structure

## Controls

- WASD or Arrow Keys: Move player
- Shift: Run
- Mouse: Camera control

## Development

The codebase is structured for easy extension:
- `app.js` - Main entry point
- `libs/` - Modular components for different game systems
- `public/assets/` - Game assets and textures
```

----------------------------------
File: app.js
Lines: 119
Tokens: 1125
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:07:32 AM
Size: 4.39 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three'; // Import necessary modules
import Stats from 'stats.js'; // Import Stats
import { loadTexture } from './libs/TextureLoader.js'; // Import the texture loader
import { setupLighting } from './libs/Lighting.js'; // Import lighting setup
import { setupCamera, setupOrbitControls, updateCameraFollow } from './libs/CameraSetup.js'; // Import camera setup and orbit controls setup
import { initInputHandler, moveState } from './libs/InputHandler.js'; // Input handling
import { createPlayer, updatePlayerMovement, targetPlayerQuaternion } from './libs/Player.js'; // Import player functions and target quaternion
import { createPushableBlock } from './libs/PushableBlock.js'; // Block creation
import { createSkySphere } from './libs/SkySphere.js'; // Sky environment
import { setupRenderer } from './libs/RendererSetup.js'; // Renderer configuration
import { createGround } from './libs/Ground.js'; // Import ground function
import { GRAVITY } from './libs/PhysicsConfig.js'; // Import physics config for coins
import { initSoundSystem } from './libs/Sound.js'; // Import sound system
import { initTailSystem } from './libs/Tail.js'; // Import tail system
import { CoinManager } from './libs/CoinManager.js'; // Coin management
import { CollisionHandler } from './libs/CollisionHandler.js'; // Collision detection
import { GameLoop } from './libs/GameLoop.js'; // Game loop orchestration

// Create a scene
const scene = new THREE.Scene();

// Set up camera using the library function
const camera = setupCamera(window.innerWidth, window.innerHeight);
scene.add(camera); // Add camera to scene
camera.lookAt(scene.position); // Look at the center

// Set up a renderer using the library function
const renderer = setupRenderer();

// Add stats.js
let stats = new Stats();
stats.showPanel(0); // 0: FPS
document.body.appendChild(stats.dom);
stats.dom.style.position = 'absolute';
stats.dom.style.top = '0px';
stats.dom.style.left = '0px';
stats.dom.style.zIndex = '100';
window.statsEnabled = false;
stats.dom.style.display = 'none'; // Initially hidden

function toggleStats() {
  window.statsEnabled = !window.statsEnabled;
  stats.dom.style.display = window.statsEnabled ? 'block' : 'none';
  console.log("Stats toggled:", window.statsEnabled ? "ON" : "OFF");
}

window.addEventListener('keydown', (event) => {
  if (event.key.toLowerCase() === 'p') {
    toggleStats();
  }
});

// Set up Orbit Controls
const controls = setupOrbitControls(camera, renderer.domElement);

// Add Sky Sphere using the library function
createSkySphere(scene, renderer);

// Set up lighting using the library function
setupLighting(scene);

// Log camera and plane positions before starting (groundPlane not available here anymore)
console.log("Initial Camera Position:", camera.position);

// Initialize Rapier using dynamic import
import('@dimforge/rapier3d').then(RAPIER => {
    console.log("Rapier initialized successfully.");

    const world = new RAPIER.World(GRAVITY);
    console.log("Physics world created.");

    // Initialize sound system on first user interaction
    document.addEventListener('click', () => {
        initSoundSystem();
    }, { once: true });

    // Initialize the tail system
    initTailSystem(renderer);

    // Setup event queue for physics
    const eventQueue = new RAPIER.EventQueue(true);

    // Create the ground 
    const groundPlane = createGround(scene, world, renderer);

    // Create the player
    const { playerMesh, playerBody } = createPlayer(scene, world, renderer);
    playerBody.userData = { type: 'player' }; 

    // Setup input handler
    initInputHandler(playerBody);

    // Create the pushable block
    const { blockMesh, blockBody } = createPushableBlock(scene, world, renderer);
    blockBody.userData = { type: 'block' }; 

    // Create the coin manager
    const coinManager = new CoinManager(scene, world, camera);
    // Add player body reference to fix pause issue
    coinManager.setPlayerBody(playerBody);
    
    // Create the collision handler
    const collisionHandler = new CollisionHandler(world, coinManager);
    
    // Create and configure the game loop
    const gameLoop = new GameLoop(scene, world, eventQueue, renderer, camera, controls);
    gameLoop.setup(
        playerMesh, 
        playerBody, 
        blockMesh, 
        blockBody, 
        coinManager, 
        collisionHandler,
        stats  // Pass stats to game loop
    );
    
    // Start the game loop
    gameLoop.start();
});
```

----------------------------------
File: docs/Code Organization.md
Lines: 50
Tokens: 501
Extension: .md
Purpose: N/A
Last Modified: 4/21/2025, 7:19:34 AM
Size: 1.97 KB
----------------------------------

{{FileContent}}
```md
# Code Organization

## Structure Overview

This codebase is organized into modular components, with core game systems separated into libraries under the `libs/` directory. The main entry point is `app.js`, which imports and initializes these systems.

## Key Modules

### Core Game Structure
- **app.js**: Main entry point that initializes the game world and components
- **libs/GameLoop.js**: Manages the main game loop and coordinates system updates

### Rendering & Visuals
- **libs/RendererSetup.js**: Configures the Three.js renderer
- **libs/CameraSetup.js**: Sets up camera and orbit controls
- **libs/Lighting.js**: Handles scene lighting
- **libs/SkySphere.js**: Creates the game's sky background
- **libs/TextureLoader.js**: Utility for loading textures

### Physics & Game Objects
- **libs/PhysicsConfig.js**: Contains physics-related configuration values
- **libs/Ground.js**: Creates the ground plane
- **libs/Player.js**: Manages player creation and movement
- **libs/Coin.js**: Defines coin objects
- **libs/CoinManager.js**: Handles spawning and managing coins
- **libs/PushableBlock.js**: Implements pushable block mechanics

### Game Systems
- **libs/InputHandler.js**: Processes keyboard/mouse input
- **libs/CollisionHandler.js**: Detects and handles object collisions
- **libs/Sound.js**: Manages game audio
- **libs/Tail.js**: Implements the snake-like tail system

## Data Flow

1. **Initialization**: app.js initializes all systems and game objects
2. **Main Loop**: GameLoop.js coordinates the update cycle:
   - Process physics (Rapier)
   - Handle collisions
   - Update player movement
   - Update visual positions
   - Update tail
   - Render frame

## System Interactions

- **Player → Tail**: Player movement is recorded in position history for tail to follow
- **Coins → Tail**: When coins are collected, a new tail segment is added
- **Input → Player**: User input controls player movement
- **Physics → Visuals**: Physics engine positions are applied to Three.js meshes
```

----------------------------------
File: docs/Tech Design Player Growth.md
Lines: 79
Tokens: 750
Extension: .md
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 2.93 KB
----------------------------------

{{FileContent}}
```md
# Technical Design: Player Growth System

## Overview
This document outlines the implementation of dynamic player growth in our game. Players will grow in size as they collect coins, with color consistency between the player and their tail.

## Core Features
1. **Player Growth**: Increase player size incrementally with each coin collection
2. **Player Coloring**: Assign random bright colors to each player
3. **Tail Coloring**: Match tail segments to player color
4. **Maintain Existing Mechanics**: Preserve Shift-to-run behavior

## Implementation Plan

### Phase 1: Player Growth Mechanism
**Task 1.1: Add Growth Function**
- Add `growPlayer()` function to `libs/Player.js`
- Define growth increment per coin collection (~0.05 units)
- Implement uniform scaling on x, y, and z axes

**Task 1.2: Growth Constraints**
- Define maximum growth size to prevent players from becoming too large
- Add optional decay mechanism to gradually reduce size over time

### Phase 2: Color System
**Task 2.1: Player Color Generation**
- Modify `createPlayer()` in `libs/Player.js` to generate random bright colors
- Store color in `playerMesh.userData.color` for reference
- Implement HSL color model for better control over brightness and saturation

**Task 2.2: Color Access Helper**
- Add `getPlayerColor()` utility function
- Ensure color consistency across different components

### Phase 3: Tail Integration
**Task 3.1: Update Coin Collection Logic**
- Modify `processCoinCollection()` in `libs/CoinManager.js`
- Trigger both player growth and tail segment creation on coin collection
- Pass player mesh reference to CoinManager for access to color data

**Task 3.2: Tail Coloring**
- Update `addTailSegment()` in `libs/Tail.js` to accept color parameter
- Apply player color to new tail segments

### Phase 4: Testing & Refinement
**Task 4.1: Visual Balance Testing**
- Test different growth rates for gameplay balance
- Ensure camera behavior works well with larger player sizes

**Task 4.2: Performance Optimization**
- Monitor performance impact with many tail segments
- Implement culling or simplification for distant tail segments if needed

## Technical Considerations

### Camera Adjustments
The current camera setup in `libs/CameraSetup.js` may need adjustment to accommodate larger player models:
- Consider dynamically adjusting `minDistance` based on player size
- Review camera follow logic to maintain optimal viewing angle

### Physics Implications
- Determine if physics body should scale with visual mesh
- Consider collision performance with larger hitboxes

### Potential Future Enhancements
- Visual effects for growth (particle effects, animation)
- Size-based gameplay mechanics (larger players move slower)
- Power-ups that temporarily modify growth rate

## Dependencies
- Three.js for rendering
- Existing player, camera, and tail systems

## Implementation Timeline
- Phase 1: 1-2 days
- Phase 2: 1 day
- Phase 3: 1-2 days
- Phase 4: 1-2 days

Total estimated time: 4-7 days
```

----------------------------------
File: docs/Tech Design Slither Tail.md
Lines: 114
Tokens: 670
Extension: .md
Purpose: N/A
Last Modified: 4/21/2025, 7:22:17 AM
Size: 2.62 KB
----------------------------------

{{FileContent}}
```md
# Technical Design: Slither Tail Implementation

## Overview

This document outlines the implementation of a snake-like tail system for our game, where the player's tail grows incrementally as they collect gold coins.

## Core Mechanics

### Position History Buffer

- Maintain an array of past player positions
- Record player positions each frame
- Limit history length based on maximum expected tail segments

### Tail Segment Management

- Each tail segment follows a position from the history buffer
- Segments are spaced by a configurable number of frames
- New segments are added when gold coins are collected

### Visual Implementation

- Each segment is a separate mesh
- Segments follow with a slight delay for a natural snake-like movement
- Optional: Add scaling, rotation, and wiggle effects for visual appeal

## Implementation Details

### Data Structures

```js
// Position history array
let positionHistory = [];
const maxHistoryLength = 1000;

// Tail segments array
let tailSegments = [];
const tailSegmentSpacing = 10; // frames between segments
```

### Key Functions

#### Position History

```js
function updatePositionHistory() {
  positionHistory.unshift(playerMesh.position.clone());
  if (positionHistory.length > maxHistoryLength) {
    positionHistory.pop();
  }
}
```

#### Adding Tail Segments

```js
function addTailSegment() {
  const segmentGeometry = new THREE.BoxGeometry(0.8, 0.8, 0.8);
  const segmentMaterial = new THREE.MeshStandardMaterial({ color: 0x00ff00 });
  const segmentMesh = new THREE.Mesh(segmentGeometry, segmentMaterial);

  segmentMesh.position.copy(playerMesh.position);
  scene.add(segmentMesh);
  tailSegments.push(segmentMesh);
}
```

#### Updating Tail Positions

```js
function updateTailPositions() {
  for (let i = 0; i < tailSegments.length; i++) {
    const historyIndex = (i + 1) * tailSegmentSpacing;
    if (positionHistory[historyIndex]) {
      tailSegments[i].position.lerp(positionHistory[historyIndex], 0.4);
    }
  }
}
```

#### Integration with Coin Collection

```js
function onCoinCollected(coin) {
  scene.remove(coin);
  addTailSegment();
}
```

## Enhancements

### Segment Orientation

```js
if (positionHistory[historyIndex + 1]) {
  tailSegments[i].lookAt(positionHistory[historyIndex + 1]);
}
```

### Animated Wiggle

```js
tailSegments[i].position.x += Math.sin(performance.now() * 0.001 + i) * 0.05;
```

### Tail Tapering

```js
segmentMesh.scale.setScalar(1 - i * 0.03);
```

## Integration with Current Codebase

The implementation will integrate with our existing Three.js and Rapier physics setup. The tail segments will be visual-only with no physics colliders to maximize performance.
```

----------------------------------
File: index.html
Lines: 13
Tokens: 127
Extension: .html
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 507.00 B
----------------------------------

{{FileContent}}
```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simple Game with Three.js and Rapier</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <canvas id="radar" width="150" height="150" style="position:absolute; bottom:10px; right:10px; z-index:1000; border-radius:50%; border:2px solid rgba(255,255,255,0.3);"></canvas>
    <script src="app.js" type="module"></script>
</body>
</html>
```

----------------------------------
File: libs/CameraSetup.js
Lines: 57
Tokens: 643
Extension: .js
Purpose: N/A
Last Modified: 4/21/2025, 7:41:25 AM
Size: 2.51 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
// Import OrbitControls
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

/**
 * Sets up the perspective camera for the scene.
 * @param {number} width - The width of the viewport.
 * @param {number} height - The height of the viewport.
 * @returns {THREE.PerspectiveCamera} The configured camera.
 */
export function setupCamera(width, height) {
    const camera = new THREE.PerspectiveCamera(75, width / height, 0.1, 1000);
    camera.position.set(0, 3, 5); // Initial camera position
    // camera.lookAt(0, 0, 0); // Look at the center (will be added to scene later)
    return camera;
}

/**
 * Sets up OrbitControls for the camera.
 * @param {THREE.PerspectiveCamera} camera - The camera to control.
 * @param {HTMLCanvasElement} domElement - The renderer's canvas element.
 * @returns {OrbitControls} The configured controls instance.
 */
export function setupOrbitControls(camera, domElement) {
    const controls = new OrbitControls(camera, domElement);
    controls.enableDamping = false; // Explicitly disable
    // controls.dampingFactor = 0.05;
    // controls.screenSpacePanning = false; // Optional: adjust panning behavior
    controls.minDistance = 5; // Set zoom limits once
    controls.maxDistance = 20;
    // controls.maxPolarAngle = Math.PI / 2; // Optional: prevent looking from below ground
    return controls;
}

/**
 * Updates the camera to follow the player mesh.
 * @param {THREE.PerspectiveCamera} camera - The camera to update.
 * @param {OrbitControls} controls - The OrbitControls instance.
 * @param {THREE.Mesh} playerMesh - The player mesh to follow.
 * @param {THREE.Vector3} lookAtOffset - Offset from player position for the camera target.
 */
export function updateCameraFollow(camera, controls, playerMesh, cameraLookAtOffset) {
    const targetLookAt = new THREE.Vector3().copy(playerMesh.position).add(cameraLookAtOffset);
    camera.position.lerp(targetLookAt, 0.1); // Keep this lerp for now as requested
    // Make camera look at the slightly elevated target point
    camera.lookAt(targetLookAt);  
    // Keep vertical tilt limited
    controls.minPolarAngle = Math.PI / 6; // looking down ~45°
    controls.maxPolarAngle = Math.PI / 4.5; // maybe ~72°, slightly lower than horizontal
    // Allow zooming in/out
    controls.minDistance = 14;   // close enough
    controls.maxDistance = 40;  // far enough to see effect
    controls.target.copy(targetLookAt); // Set target directly
    controls.update(); 
}

// TODO: Add OrbitControls setup here later if needed
```

----------------------------------
File: libs/Coin.js
Lines: 66
Tokens: 625
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 2.44 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import * as RAPIER from '@dimforge/rapier3d';
// Import renamed config constants
import { COIN_RADIUS, COIN_COLOR } from './PhysicsConfig.js';

/**
 * Creates a coin item (visual mesh and physics body).
 * @param {THREE.Scene} scene - The Three.js scene.
 * @param {RAPIER.World} world - The Rapier physics world.
 * @param {THREE.Vector3} position - The starting position for the coin.
 * @returns {{mesh: THREE.Mesh, body: RAPIER.RigidBody}} 
 */
export function createCoin(scene, world, position) {
    // Create a group to hold all coin elements
    const coinGroup = new THREE.Group();
    coinGroup.position.copy(position);
    
    // Create a sphere for the main coin
    const sphereGeometry = new THREE.SphereGeometry(COIN_RADIUS * 0.7, 24, 16);
    
    // Create a glowing material
    const material = new THREE.MeshStandardMaterial({ 
        color: COIN_COLOR,
        emissive: COIN_COLOR,
        emissiveIntensity: 1.5,
        metalness: 0.5,
        roughness: 0.2,
    });
    
    const coinMesh = new THREE.Mesh(sphereGeometry, material);
    coinGroup.add(coinMesh);
    
    // Add outer glow sphere (larger but transparent)
    const glowGeometry = new THREE.SphereGeometry(COIN_RADIUS, 24, 16);
    const glowMaterial = new THREE.MeshBasicMaterial({ 
        color: COIN_COLOR,
        transparent: true,
        opacity: 0.3,
        side: THREE.DoubleSide
    });
    
    const glowMesh = new THREE.Mesh(glowGeometry, glowMaterial);
    coinGroup.add(glowMesh);
    
    // Create a point light for enhanced glow effect
    const light = new THREE.PointLight(COIN_COLOR, 1.5, 4);
    light.position.set(0, 0, 0);
    coinGroup.add(light);
    
    scene.add(coinGroup);

    // Physics body
    const bodyDesc = RAPIER.RigidBodyDesc.dynamic().setTranslation(position.x, position.y, position.z);
    const body = world.createRigidBody(bodyDesc);
    const colliderDesc = RAPIER.ColliderDesc.ball(COIN_RADIUS)
        // Explicitly enable collision events
        .setActiveEvents(RAPIER.ActiveEvents.COLLISION_EVENTS)
        // Explicitly allow collisions with ALL types
        .setActiveCollisionTypes(RAPIER.ActiveCollisionTypes.ALL);
        
    // Create the collider and immediately set its userData
    const collider = world.createCollider(colliderDesc, body);
    collider.userData = { type: 'coin', mesh: coinGroup }; // Store mesh reference here too if needed

    return { mesh: coinGroup, body }; // Return mesh and body
}
```

----------------------------------
File: libs/CoinManager.js
Lines: 214
Tokens: 2185
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:23:18 AM
Size: 8.53 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import { createCoin } from './Coin.js';
import { playCoinCollect } from './Sound.js';
import { addTailSegment } from './Tail.js';
import { MAX_COINS, COIN_DROP_HEIGHT, COIN_SPAWN_AREA_XZ } from './PhysicsConfig.js';

/**
 * Creates and manages coins in the game world
 */
export class CoinManager {
    constructor(scene, world, camera) {
        this.scene = scene;
        this.world = world;
        this.camera = camera; // Store camera reference for coin orientation
        this.activeCoins = new Map(); // Tracks active coins by body handle
        this.coinsToProcessPostCollision = []; // Queue for cleanup
        this.spawnPosition = new THREE.Vector3();
        this.playerBody = null; // Will be set later
        this.lastVelocity = null; // Store last known velocity for backup
    }

    /**
     * Sets the player body reference - important for preserving movement during coin collection
     * @param {RAPIER.RigidBody} playerBody - The player's rigid body
     */
    setPlayerBody(playerBody) {
        this.playerBody = playerBody;
    }

    /**
     * Spawns new coins if below the maximum limit
     */
    spawnCoins() {
        if (this.activeCoins.size < MAX_COINS) { 
            const randomX = (Math.random() - 0.5) * COIN_SPAWN_AREA_XZ;
            const randomZ = (Math.random() - 0.5) * COIN_SPAWN_AREA_XZ;
            this.spawnPosition.set(randomX, COIN_DROP_HEIGHT, randomZ); 
            
            const { mesh, body } = createCoin(this.scene, this.world, this.spawnPosition); 
            this.activeCoins.set(body.handle, { mesh, body }); 
        }
    }

    /**
     * Store the current player velocity for backup purposes
     * Called from game loop to track velocity before collision
     */
    updatePlayerVelocityBackup() {
        if (this.playerBody) {
            const vel = this.playerBody.linvel();
            // Only store non-zero velocities
            if (Math.abs(vel.x) > 0.01 || Math.abs(vel.z) > 0.01) {
                this.lastVelocity = { ...vel };
            }
        }
    }

    /**
     * Processes coin collision with player
     * @param {number} coinBodyHandle - The handle of the coin's rigid body
     * @param {THREE.Mesh} coinMesh - The visual mesh of the coin
     * @param {THREE.Vector3} playerPosition - The current player position
     * @returns {boolean} Whether the coin was successfully processed
     */
    processCoinCollection(coinBodyHandle, coinMesh, playerPosition) {
        // Check if we already processed this coin
        if (!this.activeCoins.has(coinBodyHandle) || 
            this.coinsToProcessPostCollision.some(item => item.bodyHandle === coinBodyHandle)) {
            console.log("Failed check: activeCoins map doesn't have handle OR already queued");
            return false;
        }
            
        // Keep reference to current velocity before we do anything else
        let currentVel = null;
        if (this.playerBody) {
            currentVel = this.playerBody.linvel();
            console.log("Current velocity at coin collection:", currentVel);
        }
            
        // Play sound effect
        playCoinCollect(0.8);
        
        // Add a tail segment at the player's current position
        // Using setTimeout to defer heavy operations by one frame
        setTimeout(() => {
            addTailSegment(this.scene, playerPosition.clone());
        }, 0);
        
        // Remove visual mesh immediately for responsiveness
        this.scene.remove(coinMesh);
        
        // Queue for physics cleanup
        this.coinsToProcessPostCollision.push({ bodyHandle: coinBodyHandle, mesh: coinMesh });
        
        // Remove from active tracking
        this.activeCoins.delete(coinBodyHandle); 
        
        // If we have a player body reference, ensure it keeps its velocity
        if (this.playerBody) {
            // Prioritize using the current velocity if available
            const velToUse = currentVel || this.lastVelocity;
            
            if (velToUse && (Math.abs(velToUse.x) > 0.01 || Math.abs(velToUse.z) > 0.01)) {
                // Apply the velocity immediately
                this.playerBody.setLinvel({ x: velToUse.x, y: velToUse.y, z: velToUse.z }, true);
                
                // Schedule velocity preservation to happen after physics are applied
                // Use multiple frames to ensure momentum is maintained
                for (let i = 1; i <= 3; i++) {
                    setTimeout(() => {
                        if (this.playerBody && this.playerBody.isValid()) {
                            // Apply slightly decreasing values to avoid complete override of physics
                            const factor = 1 - (i * 0.1);
                            this.playerBody.setLinvel({ 
                                x: velToUse.x * factor, 
                                y: velToUse.y, 
                                z: velToUse.z * factor 
                            }, true);
                            console.log(`Applied velocity preservation frame ${i}:`, velToUse.x * factor, velToUse.z * factor);
                        }
                    }, i * 16); // 16ms ~= 1 frame at 60fps
                }
            }
        }
        
        return true;
    }

    /**
     * Cleans up coins marked for removal
     */
    cleanupCoins() {
        this.coinsToProcessPostCollision.forEach(({ bodyHandle, mesh }) => {
            const body = this.world.getRigidBody(bodyHandle);
            if (body && body.isEnabled()) {
                console.log("Disabling coin body handle:", bodyHandle);
                body.setEnabled(false);
            }
             
            if(mesh.parent) {
                this.scene.remove(mesh);
            }
            
            // Dispose of mesh resources to prevent memory leaks
            if (mesh.geometry) mesh.geometry.dispose();
            if (mesh.material) {
                if (Array.isArray(mesh.material)) {
                    mesh.material.forEach(mat => mat.dispose());
                } else {
                    mesh.material.dispose();
                }
            }
            
            // Clean up child objects too
            if (mesh.children && mesh.children.length > 0) {
                mesh.children.forEach(child => {
                    if (child.geometry) child.geometry.dispose();
                    if (child.material) {
                        if (Array.isArray(child.material)) {
                            child.material.forEach(mat => mat.dispose());
                        } else {
                            child.material.dispose();
                        }
                    }
                });
            }
        });
        this.coinsToProcessPostCollision = [];
    }

    /**
     * Updates positions of active coins and applies visual effects
     */
    updateCoinPositions() {
        // Get current time for animations
        const time = Date.now() * 0.001;
        
        this.activeCoins.forEach((coinData, handle) => { 
            const body = this.world.getRigidBody(handle);
            if (body) {
                const pos = body.translation();
                coinData.mesh.position.set(pos.x, pos.y, pos.z);
                
                // Add subtle up/down floating effect
                const floatOffset = Math.sin(time * 2) * 0.1;
                coinData.mesh.position.y += floatOffset;
                
                // Add slow rotation for spheres (looks better than facing camera)
                coinData.mesh.rotation.x += 0.01;
                coinData.mesh.rotation.y += 0.005;
                
                // Add pulsating glow effect
                // Get children of the coin group (coin mesh, glow mesh, light)
                if (coinData.mesh.children.length >= 3) {
                    const coinMesh = coinData.mesh.children[0];
                    const glowMesh = coinData.mesh.children[1];
                    const light = coinData.mesh.children[2];
                    
                    // Pulse the glow mesh size
                    const pulseFactor = 1.0 + Math.sin(time * 3) * 0.1;
                    glowMesh.scale.set(pulseFactor, pulseFactor, pulseFactor);
                    
                    // Pulse the light intensity
                    light.intensity = 1.5 + Math.sin(time * 4) * 0.5;
                    
                    // Pulse the emissive intensity of the coin
                    if (coinMesh.material) {
                        coinMesh.material.emissiveIntensity = 1.0 + Math.sin(time * 3.5) * 0.3;
                    }
                }
            } 
        });
    }
}
```

----------------------------------
File: libs/Collectible.js
Lines: 34
Tokens: 379
Extension: .js
Purpose: N/A
Last Modified: 4/20/2025, 2:45:21 PM
Size: 1.48 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import * as RAPIER from '@dimforge/rapier3d';
import { COLLECTIBLE_RADIUS, COLLECTIBLE_COLOR } from './PhysicsConfig.js';

/**
 * Creates a collectible item (visual mesh and physics body).
 * @param {THREE.Scene} scene - The Three.js scene.
 * @param {RAPIER.World} world - The Rapier physics world.
 * @param {THREE.Vector3} position - The starting position for the collectible.
 * @returns {{mesh: THREE.Mesh, body: RAPIER.RigidBody}} 
 */
export function createCollectible(scene, world, position) {
    // Visual representation (bright sphere)
    const geometry = new THREE.SphereGeometry(COLLECTIBLE_RADIUS, 16, 8); // Lower poly for performance
    // Use MeshBasicMaterial for brightness, unaffected by light
    const material = new THREE.MeshBasicMaterial({ color: COLLECTIBLE_COLOR }); 
    const mesh = new THREE.Mesh(geometry, material);
    mesh.position.copy(position);
    // No shadows needed for basic collectible
    // mesh.castShadow = true; 
    scene.add(mesh);

    // Physics body
    const bodyDesc = RAPIER.RigidBodyDesc.dynamic().setTranslation(position.x, position.y, position.z);
    const body = world.createRigidBody(bodyDesc);
    const colliderDesc = RAPIER.ColliderDesc.ball(COLLECTIBLE_RADIUS);
    world.createCollider(colliderDesc, body);

    // Add user data to easily identify this as a collectible later
    body.userData = { type: 'collectible', mesh }; 

    // console.log("Collectible created at:", position);
    return { mesh, body };
}
```

----------------------------------
File: libs/CollisionHandler.js
Lines: 68
Tokens: 606
Extension: .js
Purpose: N/A
Last Modified: 4/21/2025, 2:24:35 AM
Size: 2.37 KB
----------------------------------

{{FileContent}}
```js
/**
 * Manages collisions between objects in the physics world
 */
export class CollisionHandler {
    constructor(world, coinManager) {
        this.world = world;
        this.coinManager = coinManager;
    }

    /**
     * Process collision events from the event queue
     * @param {EventQueue} eventQueue - Rapier physics event queue
     * @param {THREE.Mesh} playerMesh - Player mesh for position reference
     */
    processCollisions(eventQueue, playerMesh) {
        eventQueue.drainCollisionEvents((handle1, handle2, started) => {
            // Get the colliders involved
            const collider1 = this.world.getCollider(handle1);
            const collider2 = this.world.getCollider(handle2);

            // Skip if either collider doesn't exist
            if (!collider1 || !collider2) {
                return; 
            }

            // Get types from collider userData
            const type1 = collider1?.userData?.type;
            const type2 = collider2?.userData?.type;

            // Check for player-coin collisions
            if ((type1 === 'player' && type2 === 'coin') || (type2 === 'player' && type1 === 'coin')) {
                this.handlePlayerCoinCollision(collider1, collider2, type1, type2, started, playerMesh);
            }
            
            // Add other collision type handlers as needed
        });
    }

    /**
     * Handle collision between player and coin
     * @private
     */
    handlePlayerCoinCollision(collider1, collider2, type1, type2, started, playerMesh) {
        if (!started) return;
        
        console.log("Player-coin collision detected");
        
        // Determine which collider is the coin
        const coinCollider = (type1 === 'coin') ? collider1 : collider2;
        
        // Get the coin's body and mesh
        const parentBody = coinCollider?.parent();
        const coinBodyHandle = parentBody?.handle;
        const coinUserData = coinCollider?.userData;
        const coinMesh = coinUserData?.mesh;
        
        // Process the coin collection if we have all required data
        if (coinBodyHandle != null && coinMesh) {
            this.coinManager.processCoinCollection(
                coinBodyHandle, 
                coinMesh, 
                playerMesh.position.clone()
            );
        } else {
            console.log("Failed to process coin: missing body handle or mesh");
        }
    }
}
```

----------------------------------
File: libs/GameLoop.js
Lines: 166
Tokens: 1338
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:23:39 AM
Size: 5.23 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import { updatePlayerMovement, targetPlayerQuaternion, updatePlayerGlow } from './Player.js';
import { updatePositionHistory, updateTailPositions } from './Tail.js';
import { updateCameraFollow } from './CameraSetup.js';
import { drawRadar } from './Radar.js';

/**
 * Main game loop manager
 */
export class GameLoop {
    constructor(scene, world, eventQueue, renderer, camera, controls) {
        this.scene = scene;
        this.world = world;
        this.eventQueue = eventQueue;
        this.renderer = renderer;
        this.camera = camera;
        this.controls = controls;
        
        // Systems that will be set later
        this.playerBody = null;
        this.playerMesh = null;
        this.blockBody = null;
        this.blockMesh = null;
        this.coinManager = null;
        this.collisionHandler = null;
        this.stats = null;
        
        // Camera settings
        this.cameraLookAtOffset = new THREE.Vector3(0, 1, 0);
        
        // Bind the game loop function to maintain context
        this.loop = this.loop.bind(this);
    }
    
    /**
     * Configure required components
     */
    setup(playerMesh, playerBody, blockMesh, blockBody, coinManager, collisionHandler, stats) {
        this.playerMesh = playerMesh;
        this.playerBody = playerBody;
        this.blockMesh = blockMesh;
        this.blockBody = blockBody;
        this.coinManager = coinManager;
        this.collisionHandler = collisionHandler;
        this.stats = stats;
    }
    
    /**
     * Start the game loop
     */
    start() {
        requestAnimationFrame(this.loop);
    }
    
    /**
     * Main game loop function
     */
    loop() {
        // Begin stats monitoring if enabled
        if (this.stats && window.statsEnabled) {
            this.stats.begin();
        }
        
        // Backup player velocity before physics step (important for collision handling)
        // Moving this before any other operations to ensure we capture clean velocity
        this.coinManager.updatePlayerVelocityBackup();
        
        // Process cleanup from previous frame
        this.coinManager.cleanupCoins();
        
        // Spawn new coins
        this.coinManager.spawnCoins();
        
        // Step physics world
        this.world.step(this.eventQueue);
        
        // Process collisions
        this.collisionHandler.processCollisions(this.eventQueue, this.playerMesh);
        
        // Update player movement
        updatePlayerMovement(this.playerBody, this.camera);
        
        // Update visual positions
        this.updateVisualPositions();
        
        // Update player glow effect based on run state
        updatePlayerGlow(this.playerMesh);
        
        // Update tail
        updatePositionHistory(this.playerMesh.position);
        updateTailPositions(true); // Apply wiggle effect
        
        // Update coin positions
        this.coinManager.updateCoinPositions();
        
        // Update camera
        updateCameraFollow(this.camera, this.controls, this.playerMesh, this.cameraLookAtOffset);
        
        // Update radar display
        this.updateRadar();
        
        // Render and continue loop
        this.renderer.render(this.scene, this.camera);
        
        // End stats monitoring if enabled
        if (this.stats && window.statsEnabled) {
            this.stats.end();
        }
        
        requestAnimationFrame(this.loop);
    }
    
    /**
     * Updates the radar display with current game objects
     */
    updateRadar() {
        // Check if we have the necessary elements
        if (!this.playerMesh) return;
        
        const currentPlayerPos = this.playerMesh.position;
        const others = [];
        
        // Add block to radar if available
        if (this.blockMesh) {
            others.push({
                x: this.blockMesh.position.x,
                z: this.blockMesh.position.z,
                color: 'blue',
                type: 'block'
            });
        }
        
        // Add any active coins to the radar
        if (this.coinManager && this.coinManager.activeCoins) {
            this.coinManager.activeCoins.forEach((coinData) => {
                if (coinData.mesh) {
                    others.push({
                        x: coinData.mesh.position.x,
                        z: coinData.mesh.position.z,
                        color: 'yellow',
                        type: 'coin'
                    });
                }
            });
        }
        
        // Draw the radar with all objects
        drawRadar(currentPlayerPos, others);
    }
    
    /**
     * Update visual positions of player and block
     */
    updateVisualPositions() {
        // Update player position and rotation
        const playerPos = this.playerBody.translation();
        this.playerMesh.position.set(playerPos.x, playerPos.y, playerPos.z);
        this.playerMesh.quaternion.slerp(targetPlayerQuaternion, 0.15);
        
        // Update block position and rotation
        const blockPos = this.blockBody.translation();
        const blockRot = this.blockBody.rotation();
        this.blockMesh.position.set(blockPos.x, blockPos.y, blockPos.z);
        this.blockMesh.quaternion.set(blockRot.x, blockRot.y, blockRot.z, blockRot.w);
    }
}
```

----------------------------------
File: libs/Ground.js
Lines: 87
Tokens: 920
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 3.59 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import * as RAPIER from '@dimforge/rapier3d';
import { loadTexture } from './TextureLoader.js';

// Constants for ground size
export const GROUND_SIZE_VISUAL = 200; // Size of the visible ground plane
export const GROUND_SIZE_PHYSICS = 100; // Size of the physics collider (half the visual for x and z dimensions)

/**
 * Creates the visual ground plane and its physics collider.
 * @param {THREE.Scene} scene - The Three.js scene.
 * @param {RAPIER.World} world - The Rapier physics world.
 * @param {THREE.WebGLRenderer} renderer - The Three.js renderer (for texture loading).
 * @returns {THREE.Mesh} The ground plane mesh.
 */
export function createGround(scene, world, renderer) {
    // Load the ground texture
    const groundTexture = loadTexture('public/assets/ground-texture-2.png', renderer, 25, 25); // Repeat values kept same for now

    // Add a flat ground visuals 
    const groundGeometry = new THREE.PlaneGeometry(GROUND_SIZE_VISUAL, GROUND_SIZE_VISUAL);
    const groundMaterial = new THREE.MeshStandardMaterial({ 
        map: groundTexture,
        side: THREE.DoubleSide, 
        roughness: 0.8 // Reduce roughness slightly, keep ground less shiny than objects
    }); 
    const groundPlane = new THREE.Mesh(groundGeometry, groundMaterial);
    groundPlane.rotation.x = Math.PI / 2;
    groundPlane.receiveShadow = true; // Allow ground to receive shadows
    scene.add(groundPlane);

    // Create the ground in the physics world - make it match the visual size
    // For a cuboid, the dimensions are half-extents, so we divide by 2
    const groundColliderDesc = RAPIER.ColliderDesc.cuboid(GROUND_SIZE_VISUAL/2, 0.1, GROUND_SIZE_VISUAL/2);
    // Set the ground collider's position so its top surface is at y=0
    groundColliderDesc.setTranslation(0.0, -0.1, 0.0);
    world.createCollider(groundColliderDesc);

    // Add visual boundary markers to show the edge of the physics ground
    addGroundBoundaryMarkers(scene, GROUND_SIZE_VISUAL);

    console.log("Ground created.");
    return groundPlane;
}

/**
 * Adds visual markers at the boundary of the ground to indicate the edge of the physics area
 * @param {THREE.Scene} scene - The Three.js scene
 * @param {number} groundSize - Size of the ground plane
 */
function addGroundBoundaryMarkers(scene, groundSize) {
    const halfSize = groundSize / 2;
    const markerHeight = 0.5;
    const markerSpacing = 10; // Space between markers
    const markerGeometry = new THREE.BoxGeometry(1, markerHeight, 1);
    const markerMaterial = new THREE.MeshStandardMaterial({ 
        color: 0xaaaaaa,
        roughness: 0.7,
        transparent: true,
        opacity: 0.7
    });

    // Create markers along the perimeter
    for (let i = -halfSize; i <= halfSize; i += markerSpacing) {
        // North edge
        const markerN = new THREE.Mesh(markerGeometry, markerMaterial);
        markerN.position.set(i, markerHeight/2, -halfSize);
        scene.add(markerN);
        
        // South edge
        const markerS = new THREE.Mesh(markerGeometry, markerMaterial);
        markerS.position.set(i, markerHeight/2, halfSize);
        scene.add(markerS);
        
        // East edge (skip corners to avoid duplicates)
        if (i > -halfSize && i < halfSize) {
            const markerE = new THREE.Mesh(markerGeometry, markerMaterial);
            markerE.position.set(halfSize, markerHeight/2, i);
            scene.add(markerE);
            
            // West edge
            const markerW = new THREE.Mesh(markerGeometry, markerMaterial);
            markerW.position.set(-halfSize, markerHeight/2, i);
            scene.add(markerW);
        }
    }
}
```

----------------------------------
File: libs/InputHandler.js
Lines: 73
Tokens: 603
Extension: .js
Purpose: N/A
Last Modified: 4/20/2025, 2:31:51 PM
Size: 2.36 KB
----------------------------------

{{FileContent}}
```js
import * as RAPIER from '@dimforge/rapier3d'; // Import Rapier types if needed
// Import physics constants
import { PLAYER_JUMP_FORCE } from './PhysicsConfig.js'; 

// Movement state based on key presses (exported)
export const moveState = {
    forward: 0, // 1 for forward, 0 otherwise
    backward: 0, // 1 for backward, 0 otherwise
    left: 0, // 1 for left, 0 otherwise
    right: 0, // 1 for right, 0 otherwise
    run: 0 // 1 for run, 0 otherwise
};

// Jump settings (REMOVED - Now imported)
// const jumpForce = 5.0;

/**
 * Initializes the keyboard event listeners for player movement.
 * @param {RAPIER.RigidBody} playerBody - The player's Rapier rigid body instance.
 */
export function initInputHandler(playerBody) {
    // Key press event listeners
    window.addEventListener('keydown', (event) => {
        switch (event.key.toLowerCase()) { // Use toLowerCase for consistency
            case 'w':
                moveState.forward = 1;
                break;
            case 's':
                moveState.backward = 1;
                break;
            case 'a':
                moveState.left = 1;
                break;
            case 'd':
                moveState.right = 1;
                break;
            case 'shift':
                moveState.run = 1;
                break;
            case ' ': // Space bar for jump
                if (playerBody) { // Ensure playerBody is available
                    const currentVel = playerBody.linvel();
                    if (Math.abs(currentVel.y) < 0.1) { // Simple ground check approximation
                        // Use imported jump force
                        playerBody.applyImpulse({ x: 0, y: PLAYER_JUMP_FORCE, z: 0 }, true);
                    }
                }
                break;
        }
    });

    window.addEventListener('keyup', (event) => {
        switch (event.key.toLowerCase()) { // Use toLowerCase for consistency
            case 'w':
                moveState.forward = 0;
                break;
            case 's':
                moveState.backward = 0;
                break;
            case 'a':
                moveState.left = 0;
                break;
            case 'd':
                moveState.right = 0;
                break;
            case 'shift':
                moveState.run = 0;
                break;
        }
    });

    console.log("Input handler initialized.");
}
```

----------------------------------
File: libs/Lighting.js
Lines: 40
Tokens: 445
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:09:30 AM
Size: 1.74 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';

/**
 * Sets up the lighting for the scene.
 * Adds an ambient light and a directional light with shadow casting enabled.
 * @param {THREE.Scene} scene - The scene to add the lights to.
 */
export function setupLighting(scene) {
    // Add ambient light (Increased intensity)
    const ambientLight = new THREE.AmbientLight(0xffffff, 0.7); // Increased from 0.5
    scene.add(ambientLight);

    // Add directional light for shadows (Increased intensity)
    const directionalLight = new THREE.DirectionalLight(0xffffff, 1.0); // Increased from 0.8
    directionalLight.position.set(5, 10, 7.5); // Position the light
    directionalLight.castShadow = true; // Enable shadow casting
    scene.add(directionalLight);

    // Configure shadow properties for the light
    directionalLight.shadow.mapSize.width = 2048; // Increase from 1024 for better quality
    directionalLight.shadow.mapSize.height = 2048; // Increase from 1024 for better quality
    directionalLight.shadow.camera.near = 0.5; // default
    directionalLight.shadow.camera.far = 100; // Increase from 50 to capture more distant shadows
    
    // Increase shadow camera size to cover more area
    directionalLight.shadow.camera.left = -50;
    directionalLight.shadow.camera.right = 50;
    directionalLight.shadow.camera.top = 50;
    directionalLight.shadow.camera.bottom = -50;
    
    // Improve shadow quality and reduce shadow artifacts
    directionalLight.shadow.bias = -0.0005;
    
    // Make sure shadow auto-update is enabled
    directionalLight.shadow.autoUpdate = true;

    // Optional: Add a helper to visualize the light's shadow camera
    // const shadowHelper = new THREE.CameraHelper(directionalLight.shadow.camera);
    // scene.add(shadowHelper);
}
```

----------------------------------
File: libs/PhysicsConfig.js
Lines: 32
Tokens: 343
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 1.34 KB
----------------------------------

{{FileContent}}
```js
/**
 * Physics configuration settings for the game.
 */
import { GROUND_SIZE_VISUAL } from './Ground.js';

// World settings
export const GRAVITY = { x: 0.0, y: -9.81, z: 0.0 };

// Player movement settings
export const PLAYER_SPEED = 5.0;
export const PLAYER_RUN_MULTIPLIER = 2.0;
export const PLAYER_JUMP_FORCE = 8.0;

// Coin settings
export const MAX_COINS = 20;
export const COIN_RADIUS = 0.3;
export const COIN_DROP_HEIGHT = 10.0;
// Limit coin spawn area to be safely within the ground boundaries
export const COIN_SPAWN_AREA_XZ = (GROUND_SIZE_VISUAL * 0.45); // 90% of ground size, divided by 2 (so +/- 45% from center)
export const COIN_COLOR = 0xffff00; // Bright yellow

// Tail settings
export const TAIL_SEGMENT_SPACING = 10; // Frames between tail segments
export const TAIL_SEGMENT_COLOR = 0x00cc99; // Default tail color
export const TAIL_MAX_SEGMENTS = 30; // Maximum number of tail segments allowed
export const TAIL_WIGGLE_AMOUNT = 0.05; // Amount of wiggle effect
export const TAIL_FOLLOW_SPEED = 0.4; // Speed at which tail segments follow (0-1)
export const TAIL_SCALE_FACTOR = 0.05; // How much each segment scales down
export const TAIL_MIN_SCALE = 0.5; // Minimum scale for tail segments
export const TAIL_SEGMENT_ROUNDNESS = 0.1; // How rounded the tail segments are

// Add other physics constants here as needed (e.g., friction, restitution)
```

----------------------------------
File: libs/Player.js
Lines: 279
Tokens: 2695
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 7:23:51 AM
Size: 10.53 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import * as RAPIER from '@dimforge/rapier3d';
import { loadTexture } from './TextureLoader.js';
import { moveState } from './InputHandler.js'; // Import moveState to use in updatePlayerMovement
// Import physics constants
import { PLAYER_SPEED, PLAYER_RUN_MULTIPLIER, TAIL_SEGMENT_COLOR, TAIL_SEGMENT_ROUNDNESS } from './PhysicsConfig.js'; 

// Reusable vectors for movement calculation
const cameraForward = new THREE.Vector3();
const cameraRight = new THREE.Vector3();
const desiredVelocity = new THREE.Vector3();
const worldUp = new THREE.Vector3(0, 1, 0);

// Exported Quaternion to store the target rotation for the visual mesh
export let targetPlayerQuaternion = new THREE.Quaternion();

// Store glow-related objects
let playerGlowMesh = null;
let glowLight = null;

// Global glow state that can be accessed by tail system
export const glowState = {
    isGlowing: false,
    intensity: 0,
    color: new THREE.Color(TAIL_SEGMENT_COLOR)
};

/**
 * Creates a procedural texture for the player, matching the tail texture
 * @returns {THREE.Texture} The generated texture
 */
function createProceduralPlayerTexture() {
    const size = 256;
    const canvas = document.createElement('canvas');
    canvas.width = size;
    canvas.height = size;
    const context = canvas.getContext('2d');
    
    // Create gradient background
    const gradient = context.createLinearGradient(0, 0, size, size);
    gradient.addColorStop(0, '#00cc99');
    gradient.addColorStop(1, '#009977');
    context.fillStyle = gradient;
    context.fillRect(0, 0, size, size);
    
    // Draw pattern of circles
    context.fillStyle = 'rgba(255, 255, 255, 0.1)';
    const circleSize = 16;
    const spacing = 32;
    
    for (let x = spacing / 2; x < size; x += spacing) {
        for (let y = spacing / 2; y < size; y += spacing) {
            context.beginPath();
            context.arc(x, y, circleSize / 2, 0, Math.PI * 2);
            context.fill();
        }
    }
    
    // Draw eyes on the front face
    context.fillStyle = 'rgba(255, 255, 255, 0.5)';
    context.beginPath();
    context.arc(size * 0.3, size * 0.4, size * 0.1, 0, Math.PI * 2);
    context.fill();
    context.beginPath();
    context.arc(size * 0.7, size * 0.4, size * 0.1, 0, Math.PI * 2);
    context.fill();
    
    // Create texture from canvas
    const texture = new THREE.CanvasTexture(canvas);
    texture.wrapS = THREE.RepeatWrapping;
    texture.wrapT = THREE.RepeatWrapping;
    
    return texture;
}

/**
 * Creates a custom geometry for the player, matching the tail segments
 * @returns {THREE.BufferGeometry} The player geometry
 */
function createPlayerGeometry() {
    // Create a slightly rounded box geometry for the player
    const geometry = new THREE.BoxGeometry(1, 1, 1, 2, 2, 2);
    
    // Get position attribute to modify vertices
    const positions = geometry.attributes.position;
    
    // Slightly round the corners by pushing vertices outward
    for (let i = 0; i < positions.count; i++) {
        const x = positions.getX(i);
        const y = positions.getY(i);
        const z = positions.getZ(i);
        
        // Calculate normalized direction from center
        const length = Math.sqrt(x*x + y*y + z*z);
        const normalizedX = x / length;
        const normalizedY = y / length;
        const normalizedZ = z / length;
        
        // Push vertices slightly outward for rounded effect (more on edges/corners)
        positions.setXYZ(
            i,
            x + normalizedX * TAIL_SEGMENT_ROUNDNESS,
            y + normalizedY * TAIL_SEGMENT_ROUNDNESS,
            z + normalizedZ * TAIL_SEGMENT_ROUNDNESS
        );
    }
    
    // Update geometry after modifying
    geometry.computeVertexNormals();
    
    return geometry;
}

/**
 * Creates the player's visual mesh and physics body.
 * @param {THREE.Scene} scene - The Three.js scene.
 * @param {RAPIER.World} world - The Rapier physics world.
 * @param {THREE.WebGLRenderer} renderer - The Three.js renderer (for texture loading).
 * @returns {{playerMesh: THREE.Mesh, playerBody: RAPIER.RigidBody}} 
 */
export function createPlayer(scene, world, renderer) {
    // Try to load SVG texture first, then fall back to procedural texture
    let playerMaterial;
    let playerColor = new THREE.Color(TAIL_SEGMENT_COLOR); // Default color
    
    try {
        const playerTexture = loadTexture('public/assets/tail-texture.svg', renderer);
        playerMaterial = new THREE.MeshStandardMaterial({
            map: playerTexture,
            roughness: 0.7
        });
    } catch (e) {
        console.log("Using procedural player texture (SVG loading failed)");
        const proceduralTexture = createProceduralPlayerTexture();
        playerMaterial = new THREE.MeshStandardMaterial({
            map: proceduralTexture,
            roughness: 0.7
        });
    }

    // Create custom player geometry that matches tail segment style
    const playerGeometry = createPlayerGeometry();
    const playerMesh = new THREE.Mesh(playerGeometry, playerMaterial);
    playerMesh.castShadow = true; // Allow player to cast shadows
    scene.add(playerMesh);
    
    // Create a single, clean glow mesh using a sphere (looks better than box)
    const glowGeometry = new THREE.SphereGeometry(1.2, 16, 16);
    const glowMaterial = new THREE.MeshBasicMaterial({
        color: playerColor,
        transparent: true,
        opacity: 0,
        side: THREE.DoubleSide,
        blending: THREE.AdditiveBlending,
        depthWrite: false // Prevent z-fighting
    });
    playerGlowMesh = new THREE.Mesh(glowGeometry, glowMaterial);
    playerMesh.add(playerGlowMesh);
    
    // Add point light for glow effect (initially turned off)
    glowLight = new THREE.PointLight(playerColor, 0, 6);
    glowLight.position.set(0, 0.5, 0); // Slight offset to create better lighting
    playerMesh.add(glowLight);
    
    // Store color for glow effects
    playerMesh.userData.glowColor = playerColor;
    glowState.color = playerColor;

    // Create a player object with physics properties
    const playerBodyDesc = RAPIER.RigidBodyDesc.dynamic().setTranslation(0.0, 1.0, 0.0);
    const playerBody = world.createRigidBody(playerBodyDesc);
    const playerColliderDesc = RAPIER.ColliderDesc.cuboid(0.5, 0.5, 0.5)
        .setActiveEvents(RAPIER.ActiveEvents.COLLISION_EVENTS)
        .setActiveCollisionTypes(RAPIER.ActiveCollisionTypes.ALL);
        
    // Create the collider and immediately set its userData
    const playerCollider = world.createCollider(playerColliderDesc, playerBody);
    playerCollider.userData = { type: 'player' };
    console.log(`Player Collider Created: handle=${playerCollider.handle}, userData=${JSON.stringify(playerCollider.userData)}`);

    console.log("Player created with tail-matching texture.");
    return { playerMesh, playerBody };
}

/**
 * Updates the player's physics body velocity based on input and camera direction.
 * @param {RAPIER.RigidBody} playerBody - The player's rigid body.
 * @param {THREE.Camera} camera - The scene camera.
 */
export function updatePlayerMovement(playerBody, camera) {
    // Get camera direction vectors
    camera.getWorldDirection(cameraForward);
    // Get right vector using cross product (Corrected order)
    cameraRight.crossVectors(cameraForward, worldUp).normalize(); 

    // Project onto XZ plane
    cameraForward.y = 0;
    cameraRight.y = 0;
    cameraForward.normalize();
    cameraRight.normalize();

    // Calculate direction based on imported moveState
    let moveDirectionZ = moveState.forward - moveState.backward;
    let moveDirectionX = moveState.right - moveState.left;
    
    // Combine directions based on camera orientation
    desiredVelocity.set(0, 0, 0); // Reset desired velocity
    desiredVelocity.addScaledVector(cameraForward, moveDirectionZ);
    desiredVelocity.addScaledVector(cameraRight, moveDirectionX);
    
    // --- Calculate Target Rotation --- 
    if (desiredVelocity.lengthSq() > 0.01) { // Only rotate if moving significantly
        desiredVelocity.normalize(); // Normalize for direction calculation
        const angle = Math.atan2(desiredVelocity.x, desiredVelocity.z); // Angle on XZ plane
        targetPlayerQuaternion.setFromAxisAngle(worldUp, angle); // Set target rotation around Y axis
    }
    // --- End Target Rotation --- 

    // Apply speed and run multiplier
    const currentSpeed = moveState.run ? PLAYER_SPEED * PLAYER_RUN_MULTIPLIER : PLAYER_SPEED;
    desiredVelocity.multiplyScalar(currentSpeed);

    // Set the rigid body's velocity
    const currentVelocity = playerBody.linvel();
    playerBody.setLinvel({ 
        x: desiredVelocity.x, 
        y: currentVelocity.y, 
        z: desiredVelocity.z 
    }, true); 
}

/**
 * Updates player's glow effect based on movement state.
 * @param {THREE.Mesh} playerMesh - The player mesh.
 */
export function updatePlayerGlow(playerMesh) {
    if (!playerGlowMesh || !glowLight) return;
    
    // Get the current time for animations
    const time = Date.now() * 0.001;
    
    if (moveState.run) {
        // Player is running - activate glow
        const pulseIntensity = 0.6 + Math.sin(time * 8) * 0.2; // Smoother, less dramatic pulsing
        
        // Smoothly transition to full glow
        const targetOpacity = 0.35 * pulseIntensity; // Lower opacity for cleaner look
        playerGlowMesh.material.opacity += (targetOpacity - playerGlowMesh.material.opacity) * 0.2;
        
        // Smooth pulse scaling
        const scaleFactor = 1.0 + Math.sin(time * 6) * 0.08; // Subtler scale pulsing
        playerGlowMesh.scale.set(scaleFactor, scaleFactor, scaleFactor);
        
        // Update light with smoother transitions
        const targetIntensity = 2.0 * pulseIntensity;
        glowLight.intensity += (targetIntensity - glowLight.intensity) * 0.2;
        
        // Update global glow state for tail system
        glowState.isGlowing = true;
        glowState.intensity = pulseIntensity;
    } else {
        // Player is not running - fade out glow
        playerGlowMesh.material.opacity *= 0.9; // Gradual fade out
        glowLight.intensity *= 0.9; // Gradual fade out for light
        
        // Reset scale smoothly when not glowing
        const currentScale = playerGlowMesh.scale.x;
        const targetScale = 1.0;
        playerGlowMesh.scale.setScalar(currentScale + (targetScale - currentScale) * 0.1);
        
        // Update global glow state
        if (playerGlowMesh.material.opacity < 0.05) {
            glowState.isGlowing = false;
            glowState.intensity = 0;
        } else {
            glowState.intensity = playerGlowMesh.material.opacity / 0.35; // Normalize to 0-1 range
        }
    }
}
```

----------------------------------
File: libs/PushableBlock.js
Lines: 36
Tokens: 435
Extension: .js
Purpose: N/A
Last Modified: 4/20/2025, 2:02:07 PM
Size: 1.70 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import * as RAPIER from '@dimforge/rapier3d';
import { loadTexture } from './TextureLoader.js';

/**
 * Creates the pushable block's visual mesh and physics body.
 * @param {THREE.Scene} scene - The Three.js scene.
 * @param {RAPIER.World} world - The Rapier physics world.
 * @param {THREE.WebGLRenderer} renderer - The Three.js renderer (for texture loading).
 * @param {THREE.Vector3} initialPosition - The starting position for the block.
 * @returns {{blockMesh: THREE.Mesh, blockBody: RAPIER.RigidBody}}
 */
export function createPushableBlock(scene, world, renderer, initialPosition = new THREE.Vector3(2, 0.5, 0)) {
    // Load the block texture
    const blockTexture = loadTexture('public/assets/block-texture.png', renderer);

    // Create visual representation for the block
    const blockGeometry = new THREE.BoxGeometry(1, 1, 1);
    const blockMaterial = new THREE.MeshStandardMaterial({ 
        map: blockTexture, // Apply the texture
        roughness: 0.5     // Reduce roughness
    });
    const blockMesh = new THREE.Mesh(blockGeometry, blockMaterial);
    blockMesh.castShadow = true; // Allow block to cast shadows
    blockMesh.position.copy(initialPosition); // Set initial visual position
    scene.add(blockMesh);

    // Create pushable block physics body and collider
    const blockBodyDesc = RAPIER.RigidBodyDesc.dynamic().setTranslation(initialPosition.x, initialPosition.y, initialPosition.z);
    const blockBody = world.createRigidBody(blockBodyDesc);
    const blockColliderDesc = RAPIER.ColliderDesc.cuboid(0.5, 0.5, 0.5);
    world.createCollider(blockColliderDesc, blockBody);
    
    console.log("Pushable block created.");
    return { blockMesh, blockBody };
}
```

----------------------------------
File: libs/Radar.js
Lines: 116
Tokens: 977
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 3.82 KB
----------------------------------

{{FileContent}}
```js
/**
 * Radar system for displaying player and other entities on a mini-map.
 */
import { GROUND_SIZE_VISUAL } from './Ground.js';

/**
 * Draws the radar showing player position and nearby entities.
 * @param {THREE.Vector3} currentPlayer - Current player position
 * @param {Array} others - Array of objects with x, z, and color properties
 */
export function drawRadar(currentPlayer, others) {
    const canvas = document.getElementById('radar');
    if (!canvas) return; // Safety check

    const ctx = canvas.getContext('2d');
    const radarSize = canvas.width;
    const radarRadius = radarSize / 2;
    
    // Adjust scale based on ground size
    const radarScale = radarSize / (GROUND_SIZE_VISUAL * 1.2); // Scale to show slightly more than the ground

    // Clear the radar
    ctx.clearRect(0, 0, radarSize, radarSize);

    // Draw background
    ctx.fillStyle = "rgba(0, 0, 0, 0.7)"; // Semi-transparent background
    ctx.beginPath();
    ctx.arc(radarRadius, radarRadius, radarRadius, 0, 2 * Math.PI);
    ctx.fill();
    
    // Draw radar grid
    ctx.strokeStyle = "rgba(0, 255, 0, 0.2)";
    ctx.lineWidth = 1;
    
    // Draw concentric circles
    for (let i = 1; i <= 3; i++) {
        ctx.beginPath();
        ctx.arc(radarRadius, radarRadius, radarRadius * i / 3, 0, 2 * Math.PI);
        ctx.stroke();
    }
    
    // Draw crosshairs
    ctx.beginPath();
    ctx.moveTo(radarRadius, 0);
    ctx.lineTo(radarRadius, radarSize);
    ctx.moveTo(0, radarRadius);
    ctx.lineTo(radarSize, radarRadius);
    ctx.stroke();
    
    // Draw ground boundary
    const halfGroundSize = GROUND_SIZE_VISUAL / 2;
    const groundCorners = [
        { x: -halfGroundSize, z: -halfGroundSize }, // NW
        { x: halfGroundSize, z: -halfGroundSize },  // NE
        { x: halfGroundSize, z: halfGroundSize },   // SE
        { x: -halfGroundSize, z: halfGroundSize }   // SW
    ];
    
    // Transform ground corners to radar coordinates
    const radarCorners = groundCorners.map(corner => {
        const dx = (corner.x - currentPlayer.x) * radarScale;
        const dz = (corner.z - currentPlayer.z) * radarScale;
        return {
            x: radarRadius + dx,
            z: radarRadius + dz
        };
    });
    
    // Draw ground boundary
    ctx.strokeStyle = "rgba(255, 255, 255, 0.5)";
    ctx.lineWidth = 2;
    ctx.beginPath();
    ctx.moveTo(radarCorners[0].x, radarCorners[0].z);
    for (let i = 1; i < radarCorners.length; i++) {
        ctx.lineTo(radarCorners[i].x, radarCorners[i].z);
    }
    ctx.closePath();
    ctx.stroke();

    // Draw self (center)
    ctx.fillStyle = "lime";
    ctx.beginPath();
    ctx.arc(radarRadius, radarRadius, 5, 0, 2 * Math.PI);
    ctx.fill();

    // Draw other players/NPCs
    others.forEach(obj => {
        const dx = (obj.x - currentPlayer.x) * radarScale;
        const dz = (obj.z - currentPlayer.z) * radarScale;

        if (Math.hypot(dx, dz) < radarRadius) {
            ctx.fillStyle = obj.color || 'red';
            ctx.beginPath();
            ctx.arc(radarRadius + dx, radarRadius + dz, 3, 0, 2 * Math.PI);
            ctx.fill();
        }
    });
    
    // Draw coins with a pulsing effect if available
    if (others.some(obj => obj.type === 'coin')) {
        const time = Date.now() * 0.001;
        const pulseScale = 0.7 + Math.sin(time * 3) * 0.3; // Pulsing effect
        
        others.filter(obj => obj.type === 'coin').forEach(coin => {
            const dx = (coin.x - currentPlayer.x) * radarScale;
            const dz = (coin.z - currentPlayer.z) * radarScale;
            
            if (Math.hypot(dx, dz) < radarRadius) {
                ctx.fillStyle = "rgba(255, 255, 0, 0.7)"; // Yellow for coins
                ctx.beginPath();
                ctx.arc(radarRadius + dx, radarRadius + dz, 2 * pulseScale, 0, 2 * Math.PI);
                ctx.fill();
            }
        });
    }
}
```

----------------------------------
File: libs/RendererSetup.js
Lines: 33
Tokens: 247
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:09:40 AM
Size: 987.00 B
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';

/**
 * Creates and configures the WebGL renderer.
 * @returns {THREE.WebGLRenderer} The configured renderer instance.
 */
export function setupRenderer() {
    const renderer = new THREE.WebGLRenderer({ 
        antialias: true,
        powerPreference: "high-performance"
    });
    renderer.setSize(window.innerWidth, window.innerHeight);
    
    // Enhanced shadow settings
    renderer.shadowMap.enabled = true;
    renderer.shadowMap.type = THREE.PCFSoftShadowMap;
    renderer.shadowMap.autoUpdate = true;
    
    // Additional quality settings
    renderer.gammaOutput = true;
    renderer.gammaFactor = 2.2;
    renderer.physicallyCorrectLights = true;
    
    document.body.appendChild(renderer.domElement);
    console.log("Renderer created and configured.");
    
    // Handle window resize
    window.addEventListener('resize', () => {
        renderer.setSize(window.innerWidth, window.innerHeight);
    });
    
    return renderer;
}
```

----------------------------------
File: libs/SkySphere.js
Lines: 21
Tokens: 224
Extension: .js
Purpose: N/A
Last Modified: 4/20/2025, 3:39:15 PM
Size: 897.00 B
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import { loadTexture } from './TextureLoader.js';

/**
 * Creates and adds a sky sphere to the scene.
 * @param {THREE.Scene} scene - The Three.js scene.
 * @param {THREE.WebGLRenderer} renderer - The Three.js renderer (for texture loading).
 */
export function createSkySphere(scene, renderer) {
    const skyTexture = loadTexture('public/assets/sky-texture.png', renderer);
    const skyGeometry = new THREE.SphereGeometry(500, 60, 40); // Large sphere
    // Invert the geometry on the x-axis so that all of the faces point inward
    skyGeometry.scale(-1, 1, 1);
    const skyMaterial = new THREE.MeshBasicMaterial({ 
        map: skyTexture, 
        side: THREE.FrontSide // Ensure material is visible from the inside
    }); 
    const skySphere = new THREE.Mesh(skyGeometry, skyMaterial);
    scene.add(skySphere);
    console.log("Sky sphere created.");
}
```

----------------------------------
File: libs/Sound.js
Lines: 68
Tokens: 480
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 1.88 KB
----------------------------------

{{FileContent}}
```js
// Sound library for game audio effects

// Initialize audio context
let audioContext;
const sounds = {};

// Initialize sound system
export function initSoundSystem() {
    // Create audio context on first user interaction
    if (!audioContext) {
        audioContext = new (window.AudioContext || window.webkitAudioContext)();
        loadSounds();
    }
    return audioContext;
}

// Load all game sounds
function loadSounds() {
    loadSound('coinCollect', 'public/coin-collect.mp3');
    // Add more sounds here as needed
}

// Load a single sound
function loadSound(name, url) {
    fetch(url)
        .then(response => response.arrayBuffer())
        .then(arrayBuffer => audioContext.decodeAudioData(arrayBuffer))
        .then(audioBuffer => {
            sounds[name] = audioBuffer;
            console.log(`Sound loaded: ${name}`);
        })
        .catch(error => console.error(`Error loading sound ${name}:`, error));
}

// Play a loaded sound with optional volume (0.0 to 1.0)
export function playSound(name, volume = 1.0) {
    if (!audioContext) {
        console.warn('Audio context not initialized. Call initSoundSystem first.');
        return;
    }
    
    const sound = sounds[name];
    if (!sound) {
        console.warn(`Sound "${name}" not loaded yet`);
        return;
    }
    
    const source = audioContext.createBufferSource();
    source.buffer = sound;
    
    // Create gain node for volume control
    const gainNode = audioContext.createGain();
    gainNode.gain.value = volume;
    
    // Connect nodes: source -> gain -> destination
    source.connect(gainNode);
    gainNode.connect(audioContext.destination);
    
    // Play the sound
    source.start(0);
    
    return source; // Return source for potential future control
}

// Specific function for coin collection sound
export function playCoinCollect(volume = 1.0) {
    return playSound('coinCollect', volume);
}
```

----------------------------------
File: libs/Tail.js
Lines: 399
Tokens: 3458
Extension: .js
Purpose: N/A
Last Modified: 4/22/2025, 8:00:15 AM
Size: 13.51 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';
import { loadTexture } from './TextureLoader.js';
import { 
    TAIL_SEGMENT_SPACING, 
    TAIL_SEGMENT_COLOR, 
    TAIL_MAX_SEGMENTS, 
    TAIL_WIGGLE_AMOUNT,
    TAIL_FOLLOW_SPEED,
    TAIL_SCALE_FACTOR,
    TAIL_MIN_SCALE,
    TAIL_SEGMENT_ROUNDNESS
} from './PhysicsConfig.js';
import { glowState } from './Player.js'; // Import glow state from Player

// Data structures for tail management
let positionHistory = [];
let tailSegments = [];
const maxHistoryLength = 1000; // Maximum length of position history buffer
let tailMaterial = null;

// Store references to glow objects for each tail segment
const tailGlowObjects = [];

// Performance optimization - limit number of active lights
const MAX_ACTIVE_LIGHTS = 5; // Only the first few segments will have actual lights

/**
 * Creates a procedural texture for tail segments
 * @returns {THREE.Texture} The generated texture
 */
function createProceduralTailTexture() {
    const size = 256;
    const canvas = document.createElement('canvas');
    canvas.width = size;
    canvas.height = size;
    const context = canvas.getContext('2d');
    
    // Create gradient background
    const gradient = context.createLinearGradient(0, 0, size, size);
    gradient.addColorStop(0, '#00cc99');
    gradient.addColorStop(1, '#009977');
    context.fillStyle = gradient;
    context.fillRect(0, 0, size, size);
    
    // Draw pattern of circles
    context.fillStyle = 'rgba(255, 255, 255, 0.1)';
    const circleSize = 16;
    const spacing = 32;
    
    for (let x = spacing / 2; x < size; x += spacing) {
        for (let y = spacing / 2; y < size; y += spacing) {
            context.beginPath();
            context.arc(x, y, circleSize / 2, 0, Math.PI * 2);
            context.fill();
        }
    }
    
    // Create texture from canvas
    const texture = new THREE.CanvasTexture(canvas);
    texture.wrapS = THREE.RepeatWrapping;
    texture.wrapT = THREE.RepeatWrapping;
    
    return texture;
}

/**
 * Creates a custom geometry for tail segments
 * @returns {THREE.BufferGeometry} The tail segment geometry
 */
function createTailSegmentGeometry() {
    // Create a slightly rounded box geometry for the tail segments
    const geometry = new THREE.BoxGeometry(0.8, 0.8, 0.8, 2, 2, 2);
    
    // Get position attribute to modify vertices
    const positions = geometry.attributes.position;
    
    // Slightly round the corners by pushing vertices outward
    for (let i = 0; i < positions.count; i++) {
        const x = positions.getX(i);
        const y = positions.getY(i);
        const z = positions.getZ(i);
        
        // Calculate normalized direction from center
        const length = Math.sqrt(x*x + y*y + z*z);
        const normalizedX = x / length;
        const normalizedY = y / length;
        const normalizedZ = z / length;
        
        // Push vertices slightly outward for rounded effect (more on edges/corners)
        positions.setXYZ(
            i,
            x + normalizedX * TAIL_SEGMENT_ROUNDNESS,
            y + normalizedY * TAIL_SEGMENT_ROUNDNESS,
            z + normalizedZ * TAIL_SEGMENT_ROUNDNESS
        );
    }
    
    // Update geometry after modifying
    geometry.computeVertexNormals();
    
    return geometry;
}

/**
 * Initializes the tail system.
 * @param {THREE.WebGLRenderer} renderer - The renderer for texture loading
 * @returns {Object} The tail system instance
 */
export function initTailSystem(renderer) {
    // Create texture and material
    try {
        // Try to load SVG directly first
        const tailTexture = loadTexture('public/assets/tail-texture.svg', renderer);
        tailMaterial = new THREE.MeshStandardMaterial({ 
            map: tailTexture,
            roughness: 0.7
        });
    } catch (e) {
        console.log("Using procedural tail texture (SVG loading failed)");
        // Fall back to procedural texture
        const proceduralTexture = createProceduralTailTexture();
        tailMaterial = new THREE.MeshStandardMaterial({ 
            map: proceduralTexture,
            roughness: 0.7
        });
    }
    
    // Clear any existing segments (in case of reinit)
    positionHistory = [];
    tailSegments = [];
    
    return {
        updatePositionHistory,
        addTailSegment,
        updateTailPositions
    };
}

/**
 * Updates the position history buffer with current player position.
 * @param {THREE.Vector3} playerPosition - Current position of the player
 */
export function updatePositionHistory(playerPosition) {
    // Store a clone to avoid reference issues
    positionHistory.unshift(playerPosition.clone());
    
    // Limit history length
    if (positionHistory.length > maxHistoryLength) {
        positionHistory.pop();
    }
}

/**
 * Creates a visual effect when a tail segment is added.
 * @param {THREE.Scene} scene - The scene
 * @param {THREE.Vector3} position - Position of the effect
 */
function createAddEffectAt(scene, position) {
    // Create a simple particles ring effect
    const particleCount = 15;
    const geometry = new THREE.BufferGeometry();
    const positions = new Float32Array(particleCount * 3);
    
    // Populate positions array with particles in a ring
    for (let i = 0; i < particleCount; i++) {
        const angle = (i / particleCount) * Math.PI * 2;
        const radius = 0.7;
        
        positions[i * 3] = position.x + Math.cos(angle) * radius;     // x
        positions[i * 3 + 1] = position.y;                            // y
        positions[i * 3 + 2] = position.z + Math.sin(angle) * radius; // z
    }
    
    geometry.setAttribute('position', new THREE.BufferAttribute(positions, 3));
    
    // Create particle material with custom color matching tail
    const particleMaterial = new THREE.PointsMaterial({
        color: TAIL_SEGMENT_COLOR,
        size: 0.2,
        transparent: true,
        opacity: 1.0
    });
    
    // Create particles system
    const particles = new THREE.Points(geometry, particleMaterial);
    scene.add(particles);
    
    // Animate and remove
    const startTime = performance.now();
    const animate = () => {
        const elapsedTime = performance.now() - startTime;
        const duration = 700; // milliseconds
        
        if (elapsedTime < duration) {
            const scale = 1 + (elapsedTime / duration) * 2;
            particles.scale.set(scale, scale, scale);
            
            particleMaterial.opacity = 1 - (elapsedTime / duration);
            
            requestAnimationFrame(animate);
        } else {
            // Remove when done
            scene.remove(particles);
            particles.geometry.dispose();
            particleMaterial.dispose();
        }
    };
    
    animate();
}

/**
 * Adds a new tail segment to the player.
 * @param {THREE.Scene} scene - The scene to add the segment to
 * @param {THREE.Vector3} initialPosition - Initial position for the segment
 * @returns {THREE.Mesh} The created tail segment mesh
 */
export function addTailSegment(scene, initialPosition) {
    // Check if we've reached max tail segments
    if (tailSegments.length >= TAIL_MAX_SEGMENTS) {
        console.log("Maximum tail length reached");
        return null;
    }
    
    // Create a custom segment geometry
    const segmentGeometry = createTailSegmentGeometry();
    const segmentMesh = new THREE.Mesh(segmentGeometry, tailMaterial);
    
    // Position at current player position initially
    segmentMesh.position.copy(initialPosition);
    
    // Apply scale based on position in tail (make segments get smaller)
    const scaleFactor = Math.max(TAIL_MIN_SCALE, 1 - tailSegments.length * TAIL_SCALE_FACTOR);
    segmentMesh.scale.setScalar(scaleFactor);
    
    // Add slight rotation to make the tail more interesting
    segmentMesh.rotation.y = Math.random() * Math.PI * 0.25;
    
    // Add shadow casting
    segmentMesh.castShadow = true;
    
    // Create glow objects for this segment
    const glowGeometry = new THREE.BoxGeometry(1.2, 1.2, 1.2);
    const glowMaterial = new THREE.MeshBasicMaterial({
        color: glowState.color,
        transparent: true,
        opacity: 0,
        side: THREE.DoubleSide,
        blending: THREE.AdditiveBlending
    });
    
    const glowMesh = new THREE.Mesh(glowGeometry, glowMaterial);
    segmentMesh.add(glowMesh);
    
    // Only add lights to the first few segments (performance optimization)
    let glowLight = null;
    if (tailSegments.length < MAX_ACTIVE_LIGHTS) {
        glowLight = new THREE.PointLight(glowState.color, 0, 3);
        segmentMesh.add(glowLight);
    }
    
    // Store references to glow objects
    tailGlowObjects.push({
        segment: segmentMesh,
        glowMesh: glowMesh,
        light: glowLight,
        index: tailSegments.length // Store index for intensity calculation
    });
    
    // Add to scene and segment array
    scene.add(segmentMesh);
    tailSegments.push(segmentMesh);
    
    // Create visual effect at the segment position
    createAddEffectAt(scene, initialPosition);
    
    return segmentMesh;
}

/**
 * Updates positions of all tail segments based on position history.
 * @param {boolean} applyWiggle - Whether to apply wiggle effect to segments
 */
export function updateTailPositions(applyWiggle = true) {
    // Update tail segments
    for (let i = 0; i < tailSegments.length; i++) {
        // Calculate history index based on spacing (index in position history)
        const historyIndex = i * TAIL_SEGMENT_SPACING;
        
        // If we have enough history for this segment
        if (historyIndex < positionHistory.length) {
            const targetPosition = positionHistory[historyIndex];
            const segment = tailSegments[i];
            
            // Calculate interpolated position toward history point
            segment.position.lerp(targetPosition, TAIL_FOLLOW_SPEED);
            
            // Add a slight wiggle effect
            if (applyWiggle) {
                const time = Date.now() * 0.001;
                const wiggleX = Math.sin(time * 2 + i * 0.5) * TAIL_WIGGLE_AMOUNT;
                const wiggleZ = Math.cos(time * 2 + i * 0.5) * TAIL_WIGGLE_AMOUNT;
                
                segment.position.x += wiggleX;
                segment.position.z += wiggleZ;
            }
        }
    }
    
    // Update tail glow based on player glow state
    updateTailGlow();
}

/**
 * Updates the glow effect on all tail segments based on the player's glow state.
 */
function updateTailGlow() {
    if (!tailGlowObjects.length) return;
    
    // Calculate fade factor based on segment position
    const fadeFactor = 0.85; // Intensity reduction per segment
    
    tailGlowObjects.forEach((glowObj, index) => {
        if (glowState.isGlowing) {
            // Calculate reduced intensity based on position in tail
            const segmentIntensity = glowState.intensity * Math.pow(fadeFactor, index);
            
            // Skip segments that would have very low intensity or are too far back
            if (segmentIntensity < 0.05 || index > TAIL_MAX_SEGMENTS / 2) {
                if (glowObj.glowMesh) glowObj.glowMesh.material.opacity = 0;
                if (glowObj.light) glowObj.light.intensity = 0;
                return;
            }
            
            // Apply glow with intensity that decreases down the tail
            if (glowObj.glowMesh) {
                glowObj.glowMesh.material.opacity = 0.4 * segmentIntensity;
                
                // Scale the glow mesh based on the segment's scale
                const baseScale = glowObj.segment.scale.x;
                glowObj.glowMesh.scale.set(1.2 * baseScale, 1.2 * baseScale, 1.2 * baseScale);
            }
            
            // Light intensity also decreases down the tail (but only for segments with lights)
            if (glowObj.light) {
                glowObj.light.intensity = 1.0 * segmentIntensity;
            }
            
            // Adjust color to gradually change down the tail (optional effect)
            if (index > 0 && glowObj.glowMesh) {
                // Slight hue shift for segments further back
                const hueShift = (index / tailGlowObjects.length) * 0.2;
                const color = new THREE.Color(glowState.color);
                color.offsetHSL(hueShift, 0, 0);
                glowObj.glowMesh.material.color = color;
                if (glowObj.light) glowObj.light.color = color;
            }
        } else {
            // Fade out glow when player is not glowing
            if (glowObj.glowMesh) glowObj.glowMesh.material.opacity *= 0.85;
            if (glowObj.light) glowObj.light.intensity *= 0.85;
        }
    });
}

/**
 * Gets the current number of tail segments.
 * @returns {number} Current tail length
 */
export function getTailLength() {
    return tailSegments.length;
}

/**
 * Resets the tail by removing all segments.
 * @param {THREE.Scene} scene - The scene containing the segments
 */
export function resetTail(scene) {
    // Remove all tail segments from the scene
    tailSegments.forEach(segment => {
        scene.remove(segment);
        // Manually dispose geometries and materials to prevent memory leaks
        if (segment.geometry) segment.geometry.dispose();
        if (segment.material) segment.material.dispose();
        
        // Dispose of child meshes
        if (segment.children && segment.children.length > 0) {
            segment.children.forEach(child => {
                if (child.geometry) child.geometry.dispose();
                if (child.material) child.material.dispose();
            });
        }
    });
    
    // Clear arrays
    tailSegments = [];
    tailGlowObjects.length = 0; // Clear glow objects array
    positionHistory = [];
}
```

----------------------------------
File: libs/TextureLoader.js
Lines: 31
Tokens: 284
Extension: .js
Purpose: N/A
Last Modified: 4/20/2025, 9:13:01 AM
Size: 1.11 KB
----------------------------------

{{FileContent}}
```js
import * as THREE from 'three';

/**
 * Loads a texture from the given URL.
 * Configures repeat wrapping suitable for ground textures.
 *
 * @param {string} url - The path to the texture image.
 * @param {THREE.WebGLRenderer} renderer - The Three.js renderer instance.
 * @param {number} [repeatX=1] - How many times the texture should repeat horizontally.
 * @param {number} [repeatY=1] - How many times the texture should repeat vertically.
 * @returns {THREE.Texture} The configured texture object.
 */
export function loadTexture(url, renderer, repeatX = 1, repeatY = 1) {
    const loader = new THREE.TextureLoader();
    const texture = loader.load(url);

    // Set wrapping to repeat
    texture.wrapS = THREE.RepeatWrapping;
    texture.wrapT = THREE.RepeatWrapping;

    // Set repetition amount
    texture.repeat.set(repeatX, repeatY);

    // Optional: Improve texture quality
    texture.magFilter = THREE.LinearFilter;
    texture.minFilter = THREE.LinearMipmapLinearFilter;
    // Use the passed renderer instance
    texture.anisotropy = Math.min(16, renderer.capabilities.getMaxAnisotropy());

    return texture;
}
```

----------------------------------
File: libs/three.min.js
Lines: 90
Tokens: 2345
Extension: .js
Purpose: N/A
Last Modified: 4/20/2025, 12:22:29 AM
Size: 9.16 KB
----------------------------------

{{FileContent}}
```js
<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-type" content="text/html; charset=utf-8">
    <meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'unsafe-inline'; img-src data:; connect-src 'self'">
    <title>Page not found &middot; GitHub Pages</title>
    <style type="text/css" media="screen">
      body {
        background-color: #f1f1f1;
        margin: 0;
        font-family: "Helvetica Neue", Helvetica, Arial, sans-serif;
      }

      .container { margin: 50px auto 40px auto; width: 600px; text-align: center; }

      a { color: #4183c4; text-decoration: none; }
      a:hover { text-decoration: underline; }

      h1 { width: 800px; position:relative; left: -100px; letter-spacing: -1px; line-height: 60px; font-size: 60px; font-weight: 100; margin: 0px 0 50px 0; text-shadow: 0 1px 0 #fff; }
      p { color: rgba(0, 0, 0, 0.5); margin: 20px 0; line-height: 1.6; }

      ul { list-style: none; margin: 25px 0; padding: 0; }
      li { display: table-cell; font-weight: bold; width: 1%; }

      .logo { display: inline-block; margin-top: 35px; }
      .logo-img-2x { display: none; }
      @media
      only screen and (-webkit-min-device-pixel-ratio: 2),
      only screen and (   min--moz-device-pixel-ratio: 2),
      only screen and (     -o-min-device-pixel-ratio: 2/1),
      only screen and (        min-device-pixel-ratio: 2),
      only screen and (                min-resolution: 192dpi),
      only screen and (                min-resolution: 2dppx) {
        .logo-img-1x { display: none; }
        .logo-img-2x { display: inline-block; }
      }

      #suggestions {
        margin-top: 35px;
        color: #ccc;
      }
      #suggestions a {
        color: #666666;
        font-weight: 200;
        font-size: 14px;
        margin: 0 10px;
      }

    </style>
  </head>
  <body>

    <div class="container">

      <h1>404</h1>
      <p><strong>File not found</strong></p>

      <p>
        The site configured at this address does not
        contain the requested file.
      </p>

      <p>
        If this is your site, make sure that the filename case matches the URL
        as well as any file permissions.<br>
        For root URLs (like <code>http://example.com/</code>) you must provide an
        <code>index.html</code> file.
      </p>

      <p>
        <a href="https://help.github.com/pages/">Read the full documentation</a>
        for more information about using <strong>GitHub Pages</strong>.
      </p>

      <div id="suggestions">
        <a href="https://githubstatus.com">GitHub Status</a> &mdash;
        <a href="https://twitter.com/githubstatus">@githubstatus</a>
      </div>

      <a href="/" class="logo logo-img-1x">
        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
      </a>

      <a href="/" class="logo logo-img-2x">
        <img width="32" height="32" title="" alt="" src="data:image/png;base64,">
      </a>
    </div>
  </body>
</html>
```

----------------------------------
File: package.json
Lines: 23
Tokens: 111
Extension: .json
Purpose: N/A
Last Modified: 4/22/2025, 8:03:25 AM
Size: 445.00 B
----------------------------------

{{FileContent}}
```json
{
  "name": "simple-game-blocks",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "vite"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@dimforge/rapier3d": "^0.15.1",
    "stats.js": "^0.17.0",
    "three": "^0.175.0"
  },
  "devDependencies": {
    "vite": "^6.3.2",
    "vite-plugin-wasm": "^3.4.1"
  }
}
```

----------------------------------
File: public/style.css
Lines: 10
Tokens: 64
Extension: .css
Purpose: N/A
Last Modified: 4/21/2025, 2:25:49 AM
Size: 256.00 B
----------------------------------

{{FileContent}}
```css
html, body {
    margin: 0;
    padding: 0;
    overflow: hidden; /* Prevent scrollbars if canvas slightly overlaps */
    height: 100%; /* Ensure body takes full height */
}

canvas {
    display: block; /* Prevent potential extra space below canvas */
}
```

----------------------------------
File: vite.config.js
Lines: 6
Tokens: 32
Extension: .js
Purpose: N/A
Last Modified: 4/20/2025, 8:37:35 AM
Size: 129.00 B
----------------------------------

{{FileContent}}
```js
import { defineConfig } from 'vite';
import wasm from 'vite-plugin-wasm';

export default defineConfig({
  plugins: [wasm()]
});
```


## Summary
You have completed the context generation. Quick stats below

### General Stats
Number of files: 28
Number of tokens: 22,997
Size in MB: 0.09

### Top 5 Files by Size
- libs/Tail.js: 13,830 characters, 3,458 tokens
- libs/Player.js: 10,779 characters, 2,695 tokens
- libs/three.min.js: 9,379 characters, 2,345 tokens
- libs/CoinManager.js: 8,739 characters, 2,185 tokens
- libs/GameLoop.js: 5,351 characters, 1,338 tokens

### Files Excluded
- : 4 file(s)
  - .DS_Store
  - .gitignore
  - public/.DS_Store
  - public/assets/.DS_Store
- .ico: 1 file(s)
  - favicon.ico
- .json: 1 file(s)
  - package-lock.json
- .png: 2 file(s)
  - public/assets/ground-texture.png
  - public/assets/tail-texture.png
- .svg: 1 file(s)
  - public/assets/tail-texture.svg
- .mp3: 5 file(s)
  - public/block-kill-2.mp3
  - public/block-kill.mp3
  - public/coin-collect-large.mp3
  - public/coin-collect.mp3
  - public/game-end.mp3